#include <stdio.h>
int main(){
    int a; //variable declaration
    a = 3; // variable inilation
    printf("%d", a);// %d-> datatype spacifier
    printf(" The output of this is %d", a );
    return 0;
} 